# CLAUDE.md — pocastation/frontend

이 저장소(`pocastation/frontend`)에서 작업하는 **모든 Claude 세션은 아래 규칙을 반드시 따른다.** 이슈·PR·브랜치·커밋 형식을 임의로 만들지 말 것. 단일 진실원은 kgu 스킬이며, 이 문서는 그 스킬을 반드시 호출하라는 강제 포인터다.

## 필수 워크플로우 (kgu-flow)

이슈/브랜치/PR/커밋을 만들 때는 반드시 해당 kgu 스킬을 호출해 그 템플릿을 그대로 쓴다.

- 이슈 생성 → `kgu-issue` 스킬
- 브랜치 생성·전체 사이클 → `kgu-flow` 스킬
- PR 생성 → `kgu-pr` 스킬

전체 사이클: **이슈 생성 → `{type}/{issue#}` 브랜치 → 구현 → PR → (승인 후) squash 머지 + 원격 브랜치 삭제 + 이슈 close.**

**머지는 사용자가 명시적으로 "머지"라고 지시할 때만 한다.** 그 전에는 커밋 초안·검증 결과만 제시하고 대기한다.

## 네이밍·형식 규칙 (스킬을 못 불러도 이것만은 지킨다)

- 브랜치: `{type}/{issue#}` — 예: `feature/161`, `fix/155`, `refactor/157`, `docs/167`
- 이슈 제목: `{type} : {제목}`
- PR 제목: `{type} : {이슈 제목} (#{issue})` ← **`(#issue)` 접미 누락 금지**
- 커밋 / 스쿼시 머지 제목: `{type} : {설명} (#{issue})` — 한국어
- type: `feature`(=`feat`) · `fix` · `refactor` · `chore` · `docs`
- **PR base 브랜치: `develop`** (main 직접 PR 금지)

## 이슈 본문 (kgu-issue 템플릿)

- `## 🪄 Description`
- `## ❤️ Changes` → `### 🌳 작업 사항` (`- [ ]` 체크리스트)
- `## 📊 API` — 프론트 작업은 보통 생략(신규 API 계약이 걸린 경우에만)
- `## 😃 Additional context` (선택)

## PR 본문 (kgu-pr 템플릿)

- `## #️⃣ 연관된 이슈` + `Closes #{issue}`
- `## 📝 작업 내용` — 변경 영역별 `###` 소제목, 순서 있는 로직은 화살표 나열 대신 번호목록
- `### ✨ 스크린샷` — **UI 변경이 있으면 preview 브라우저 증거 첨부**
- `## ✅ 검증` — 확인한 시나리오 체크리스트
- `## 💬 리뷰 요구사항` (선택)
- **`>`(blockquote) 남용 금지.** 헤더·목록·체크리스트로 구조화. 제목·본문 한국어.

## 이슈·PR 문체

외부 레포 8곳과 이 저장소의 기록을 같은 잣대로 재서 정한 기준이다(봇 PR·HTML 주석 제외, 레포당 38~60건, 2026-09-04 측정). 취향이 아니라 측정값이다.

### 지키는 것

- 길이는 제한하지 않는다. 무엇을(what)·왜(why) 바꿨는지 담는다. Google eng-practices의 CL 설명 지침을 따른다
- 한국어 종결은 명사형·음슴체를 먼저 쓴다. 하다체(~한다·~였다)는 이유를 적는 문장에만
- 볼드는 건당 2개 이하, em-dash(—)는 건당 1개 이하
- 색 이모지(🔴 등)를 쓰지 않는다. 섹션 헤더의 템플릿 이모지는 예외

### 담지 않는 것

- 작업 과정 회고. 「예전에는 ~였다」, 「처음엔 ~로 봤는데 틀렸다」는 대화나 코드 주석의 몫이다
- 극적 표현. 사실과 근거만 적는다
- 같은 사실의 반복. 제목·개요·검증에 같은 문장을 세 번 쓰지 않는다

### 측정 근거

| 지표 | 외부 8개 레포 | 이 저장소 초기 | 이 저장소 최근 |
| --- | --- | --- | --- |
| 본문 중앙값 | 236~2,036자 | 846자 | 1,510자 |
| 볼드 / 건 | 0.1~1.1 | 0.9 | 10.0 |
| em-dash / 건 | 0.0~1.0 | 2.7 | 7.5 |
| 하다체 / 건(한글 본문) | naver 0.0 | 0.3 | 6.0 |

길이는 이상치가 아니다. 치우친 것은 장식과 종결 문체다.

비교 대상: facebook/react, vercel/next.js, microsoft/vscode, kubernetes/kubernetes, toss/slash, toss/es-toolkit, daangn/stackflow, naver/egjs-flicking

## 검증 (PR 전 필수)

- 로컬 dev + **preview 브라우저 E2E**. 관측 가능한 변경은 preview로 실제 확인 후 증거를 제시한다.
- 검증용 임시 하니스 라우트(`app/devXXX` 등)는 **머지 전 반드시 삭제**한다.
- 삭제한 라우트가 `.next/dev/types`에 stale 참조로 남아 `tsc`가 깨지면 `rm -rf .next`로 재생성.

## 디자인 — 이 레포의 모든 UI 작업에 적용

목표는 하나다. **"AI가 만든 웹사이트"로 보이지 않고, 실제 서비스 디자이너가 오래 다듬은 상용 K-POP 마켓플레이스처럼 보이게 한다.**

이 절은 취향이 아니라 **두 번의 반려를 거쳐 확정된 기준**이다(2026-08-04~05, 가이드 화면 #257).
1차 초안은 "AI가 만든 SaaS UI"로 반려됐고 — 단계 11개를 전부 같은 카드로 감싸고 연보라 배경·원형 숫자 배지·장식 아이콘 16종·alert box 5개를 썼다.
2차는 카드를 걷어냈더니 이번엔 "AI가 만든 미니멀 편집 디자인"으로 반려됐다 — 7개 섹션이 전부 같은 골격(규칙선 → 번호 → 제목 → 본문)이라 Notion 문서처럼 읽혔다.

### 절대 하지 않는 것

- 모든 영역을 같은 카드로 감싸기 / 모든 카드에 같은 radius·box-shadow
- gradient 배경, glassmorphism
- 연보라(`primary-soft`) 배경 반복
- 원형 숫자 배지, 장식용 아이콘·이모지, 아이콘+텍스트 버튼 패턴
- 알약(pill) UI 남발
- 모든 설명을 alert box로 만들기
- 모든 정보를 같은 `label | description` 표로 만들기
- 의미 없이 큰 hero
- 균일 반복 카드 그리드 / 모든 섹션 동일 grid
- Inter풍 SaaS 타이포, shadcn 기본 조합 느낌
- "깔끔함"을 이유로 정보 밀도를 낮추거나 실제 interaction을 약하게 만들기

### 대신 하는 것

- **섹션마다 지면을 달리한다** — 투명 / 회색 배경띠 / 테두리 패널 / 규칙선 분할. 구분은 typography·whitespace·헤어라인·numbering·alignment의 조합으로 만든다
- **카드는 실제로 분리돼야 하는 콘텐츠에만.** 한 페이지에 강조 패널은 하나면 충분하다
- **정보 중요도에 따라 시각적 weight를 다르게.** 모든 섹션이 같은 패턴일 필요 없다 — **일관성과 반복을 구분한다**
- **radius를 요소별로 다르게** 써도 된다(CTA 4px / 입력 6px / 칩·슬롯 3px / 배경띠 0)
- **폼에서는 입력 UI가 설명보다 앞선다** — 라벨 13px → 입력칸 15px·높이 48px → helper 12px
- **보라(`primary`)는 상태를 말하는 자리에만** — primary CTA, 선택된 상태, 활성 탭, 필수 표시, focus. 배경·아이콘·제목에는 쓰지 않는다
- 나머지는 white / near-white / gray / ink
- 진짜 경고만 강조(좌측 규칙선). 일반 안내는 helper text로 녹인다

### 작업 순서 (강제)

1. **코드를 고치기 전에 "왜 AI가 만든 것처럼 보이는가"를 먼저 진단**하고 근거(파일·클래스)와 함께 제시한다
2. 그다음 구조를 바꾼다. **border-radius만 줄이는 수준의 수정은 요구를 충족하지 못한다**
3. 기능·데이터·validation·API는 건드리지 않는다

### 검증

브라우저에서 **수치로 확인한다** — 이 방식이 실제로 반려 원인을 잡아냈다.

```
본문 box-shadow 개수 · gradient 개수 · 이모지 개수 · 연보라 배경 개수  → 0
보라 사용처를 나열해 제목·섹션배경에 쓰이지 않았는지
섹션별 지면(배경·테두리·radius)이 실제로 갈렸는지
입력칸 높이·글자크기 vs helper 글자크기
```
