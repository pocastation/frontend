// 본인확인 대행사 표준창 호출(#323).
//
// 가입 폼과 본인인증 패널이 **같은 함수를 쓴다.** 두 곳에 따로 적으면 대행사를 붙이는 날
// 한쪽만 고쳐진 채로 남고, 그 사실은 인증이 안 되는 화면을 누가 밟아야 드러난다.

/**
 * 대행사 표준창을 띄우고 발급 식별자를 받아온다.
 *
 * 다날 연동정보(모듈·암호키)를 아직 받지 못해 비어 있다. 계약이 끝나면 여기서 표준창을 열고
 * 대행사가 돌려주는 식별자를 반환하면 된다.
 *
 * ⚠️ **인증 결과(CI·생년월일 등)를 여기서 만들어 서버로 보내면 안 된다.** 위조된 인증이 그대로
 * 저장된다. 서버가 이 식별자로 대행사에 다시 조회해 결과를 확정한다(BE #293·#297).
 * 다날 취약점 자체점검 체크리스트 2번(파라미터 변조)이 요구하는 구조가 이것이다.
 *
 * @returns 대행사 발급 식별자. 연동 전에는 항상 null.
 */
export async function openIdentityWindow(): Promise<string | null> {
  return null;
}

/** 연동 전이라 인증을 시작할 수 없을 때 사용자에게 보여줄 문구. 두 화면이 같은 말을 하게 한다. */
export const IDENTITY_NOT_READY_MESSAGE =
  "본인인증 기능을 준비하고 있어요. 준비되면 알려드릴게요.";
